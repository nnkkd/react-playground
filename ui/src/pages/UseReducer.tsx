import { Layout } from '../components/layouts/Layouts'
import { UseReducerApp } from '../components/useReducerApp/useReducerApp'

export const UseReducer = (): JSX.Element => {
    return (
        <Layout title='useReducer test'>
            <UseReducerApp />
        </Layout>
    )
}
